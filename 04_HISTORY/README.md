# 04_HISTORY

Historical APU material. Nothing in this directory is current source of truth unless a current document explicitly cites it as historical evidence.

## Policy
- Preserve historical meaning; do not rewrite old artifacts to match current architecture.
- Normal Codex implementation must not load this archive unless the task specifically requires historical context.
- Current state lives in `01_PRODUCT/`, `03_APU_SITE/`, and `05_WORKFLOW/`.
- Current versioned APU Core files live in `/apu-core/`.

## External legacy archives retained on Google Drive
The following large/backup artifacts remain external and are intentionally outside the active repository documentation set:

- `ApuSite_export` — backup of the original GPT Sites source/archive.
- `APU Site — Design System v0.1 — Baseline & Theme Presets` — large historical visual/design document (~1.4 MB).
- `2026-08-15_APU_SITE_CURRENT_STATE` — legacy Sites checkpoint bundle.

These locations are archival only. Google Drive is not part of the mandatory development synchronization chain and these artifacts must not be treated as current implementation instructions.
