# Historical audits

Legacy audits from the pre-GitHub/Cloudflare consolidation are retained as archive material. They are not automatically applicable to the current codebase.

When a historical audit is needed, retrieve the specific source deliberately rather than treating the whole archive as current context.
