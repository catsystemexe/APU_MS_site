# DEVLOG documentation

Current protocol: `APU_DEV_LOG_PROTOCOL.md`.

Canonical live feedback data is not stored in this documentation folder. It lives in `data/dev-log/entries/` and mutable status/note overrides live in Cloudflare KV through the application contract.

Older APU Shared aggregate protocol exports are historical working artifacts. They are not current source of truth and do not need to be loaded by Codex for normal implementation.
